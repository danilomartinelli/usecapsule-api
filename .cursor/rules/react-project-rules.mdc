# Regras para Projetos React no Workspace ACME

Você está trabalhando em projetos React no workspace ACME. Siga estas diretrizes específicas:

## 🏗️ **Estrutura de Projetos React**

### Organização de Arquivos

- **Apps React**: `apps/dashboard/`, `apps/landing-page/`, `apps/portal/`
- **Estrutura**: Use `app/` como pasta principal (padrão Vite + React Router)
- **Rotas**: Organize em `app/routes/` seguindo convenções do React Router
- **Componentes**: Mantenha próximos aos arquivos que os utilizam
- **Testes**: Use `tests/` na raiz de cada app

### Apps Disponíveis

- **`dashboard`**: Painel administrativo interno
- **`landing-page`**: Página de marketing/landing
- **`portal`**: Portal principal da aplicação

## 🎯 **Componentes React**

### Padrões de Componentes

```tsx
// ✅ DO: Componente funcional com TypeScript
interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'danger';
  children: React.ReactNode;
  onClick?: () => void;
  disabled?: boolean;
}

export const Button: React.FC<ButtonProps> = ({
  variant = 'primary',
  children,
  onClick,
  disabled = false,
}) => {
  const baseClasses = 'px-4 py-2 rounded font-medium transition-colors';
  const variants = {
    primary: 'bg-blue-500 text-white hover:bg-blue-600',
    secondary: 'bg-gray-200 text-gray-800 hover:bg-gray-300',
    danger: 'bg-red-500 text-white hover:bg-red-600',
  };

  return (
    <button
      className={`${baseClasses} ${variants[variant]}`}
      onClick={onClick}
      disabled={disabled}
    >
      {children}
    </button>
  );
};
```

### Hooks e Estado

- **Estado Local**: Use `useState` e `useEffect` para estado simples
- **Estado Global**: Use Context API para estado compartilhado entre componentes
- **Hooks Customizados**: Crie em `app/hooks/` para lógica reutilizável
- **Evite Prop Drilling**: Use Context ou composição quando necessário

## 🛣️ **Roteamento com React Router**

### Configuração de Rotas

```tsx
// app/routes.tsx
import { createBrowserRouter } from 'react-router-dom';
import { Root } from './root';
import { About } from './routes/about';

export const router = createBrowserRouter([
  {
    path: '/',
    element: <Root />,
    children: [
      {
        index: true,
        element: <Dashboard />,
      },
      {
        path: 'about',
        element: <About />,
      },
    ],
  },
]);
```

### Boas Práticas de Roteamento

- **Lazy Loading**: Use `lazy()` para rotas grandes
- **Rotas Protegidas**: Implemente guards para autenticação
- **URLs Limpas**: Use URLs descritivas e RESTful
- **Navegação**: Use `Link` para navegação interna

## 🎨 **Estilização com Tailwind CSS**

### Classes Utilitárias

```tsx
// ✅ DO: Organizar classes por categoria
<div className="
  /* Layout */
  flex items-center justify-between
  /* Espaçamento */
  p-4 md:p-6 lg:p-8
  /* Visual */
  bg-white rounded-lg shadow-md
  /* Interação */
  hover:shadow-lg transition-shadow
  /* Responsividade */
  w-full md:w-auto
">
```

### Responsividade

- **Mobile-First**: Comece com classes mobile, depois adicione breakpoints
- **Breakpoints**: `sm:`, `md:`, `lg:`, `xl:`, `2xl:`
- **Teste**: Sempre teste em diferentes tamanhos de tela

## 🧪 **Testes**

### Estrutura de Testes

```tsx
// tests/routes/about.spec.tsx
import { render, screen } from '@testing-library/react';
import { About } from '../app/routes/about';

describe('About', () => {
  it('should render about page', () => {
    render(<About />);
    expect(screen.getByText(/sobre/i)).toBeInTheDocument();
  });
});
```

### Boas Práticas de Teste

- **Testing Library**: Use `@testing-library/react` para testes de componentes
- **Teste Comportamento**: Teste o que o usuário vê, não implementação
- **Mocks**: Use mocks para dependências externas
- **Cobertura**: Mantenha boa cobertura de testes

## 🚀 **Desenvolvimento**

### Comandos Específicos

```bash
# Dashboard
nx serve dashboard          # Desenvolvimento
nx build dashboard          # Build produção
nx test dashboard           # Testes
nx lint dashboard           # Linting

# Landing Page
nx serve landing-page       # Desenvolvimento
nx build landing-page       # Build produção
nx test landing-page        # Testes
nx lint landing-page        # Linting

# Portal
nx serve portal             # Desenvolvimento
nx build portal             # Build produção
nx test portal              # Testes
nx lint portal              # Linting
```

### Ferramentas de Desenvolvimento

- **Vite**: Desenvolvimento rápido com hot reload
- **React DevTools**: Debugging de componentes
- **Tailwind CSS IntelliSense**: Autocomplete de classes
- **ESLint + Prettier**: Qualidade e formatação de código

## 📦 **Integração com Libraries**

### UI Components

```tsx
// ✅ DO: Usar componentes da library UI
import { Button, Card } from '@acme/ui/react';

export const Dashboard = () => {
  return (
    <Card>
      <Button variant="primary">Ação</Button>
    </Card>
  );
};
```

### Shared Types

```tsx
// ✅ DO: Usar tipos compartilhados
import { UserDto } from '@acme/shared/dto';

interface UserProfileProps {
  user: UserDto;
}
```

## 🚨 **Anti-patterns a Evitar**

### ❌ NÃO fazer:

```tsx
// ❌ DON'T: Componente de classe desnecessário
class Button extends React.Component {
  render() {
    return <button>{this.props.children}</button>;
  }
}

// ❌ DON'T: Classes CSS inline
<div style={{ padding: '1rem', backgroundColor: 'white' }}>

// ❌ DON'T: Lógica complexa em componentes
const ComplexComponent = () => {
  // 100+ linhas de lógica aqui
};
```

### ✅ Fazer:

```tsx
// ✅ DO: Componente funcional simples
const Button: React.FC<ButtonProps> = ({ children }) => (
  <button className="px-4 py-2 bg-blue-500 text-white rounded">
    {children}
  </button>
);

// ✅ DO: Classes Tailwind organizadas
<div className="p-4 bg-white rounded shadow">

// ✅ DO: Lógica em hooks customizados
const useUserData = () => {
  // Lógica complexa aqui
  return { user, loading, error };
};
```

## 🔧 **Performance**

### Otimizações

- **React.memo()**: Para componentes que re-renderizam desnecessariamente
- **useMemo/useCallback**: Para cálculos e funções custosas
- **Lazy Loading**: Para componentes e rotas grandes
- **Code Splitting**: Aproveite o Vite para divisão automática

### Monitoramento

- **React DevTools Profiler**: Analise performance de componentes
- **Bundle Analyzer**: Monitore tamanho do bundle
- **Lighthouse**: Teste performance geral da aplicação
  description:
  globs:
  alwaysApply: false

---
